# Deno Deploy 配置文件
# 部署到 Deno Deploy 时使用

# 项目信息
name: week-report-agent

version: 1.0.0
description: 职场周报生成工具

# 运行时配置
runtime: deno
entrypoint: main.ts

# 环境变量
env:
  PORT: 8000

# 权限配置
permissions:
  - net
  - read

# 构建配置
build:
  commands:
    - echo "No build step required for Deno"
